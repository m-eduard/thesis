export interface FolderIcon {
  /**
   * Name of the icon, e.g. "contests"
   */
  name: string;

  /**
   * Path to the icon, e.g. "resources/icons/folder.svg"
   */
  path: string;

  /**
   * Path to an icon depicting the open folder
   * E.g. "resources/icons/folder-open.svg"
   */
  openPath: string;
}
